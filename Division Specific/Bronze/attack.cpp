//#include "bits/stdc++.h"
////#include "kimko_stdc++.h"
//
//using namespace std;
//
//typedef long long ll;
//typedef vector<int> vi;
//typedef pair<int, int> pii;
//
//#define FOR(i, a, b) for (int i=a; i<b; i++)
//#define F0R(i, a) for (int i=0; i<a; i++)
//#define FORd(i,a,b) for (int i = (b)-1; i >= a; i--)
//#define F0Rd(i,a) for (int i = (a)-1; i >= 0; i--)
//
//#define mp make_pair
//#define pb push_back
//#define f first
//#define s second
//#define lb lower_bound
//#define ub upper_bound
//
//const int MOD = 1000000007;
//double PI = 4*atan(1);
//
//const int MAX_VAL = (int)1e9;
//
//
//int main()
//{
////    ifstream fin("sort3.txt");
////    ofstream fout("sort4.txt");
//    long long  a, ss, k, x, y;
//    cin >> a >> ss >> k >> x >> y;
//    assert(1 <= a && a <= MAX_VAL);
//    assert(1 <= ss && ss <= MAX_VAL);
//    assert(1 <= k && k <= MAX_VAL);
//    assert(1 <= x && x <= MAX_VAL);
//    assert(1 <= y && y <= MAX_VAL);
//    double ans = (double) (ss + k * y - a)/(x+y);
//    if (int(ans) == ans && ans >=0 && ans <= k){
//        cout << int(ans);
//    }
//    else{
//        cout << -1;
//    }
//
//
//    return 0;
//}